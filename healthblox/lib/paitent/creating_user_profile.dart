import 'package:flutter/material.dart';

class CreatingUserProfile extends StatefulWidget {
  const CreatingUserProfile({super.key});

  @override
  State<CreatingUserProfile> createState() => _CreatingUserProfileState();
}

class _CreatingUserProfileState extends State<CreatingUserProfile> {
  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: SingleChildScrollView(
        child: Column(),
      )
    );
  }
}